import React from 'react';

import { View, Text } from 'react-native';

const ListPlaces = () => {
    return (
        <View>
            <Text>ListPlaces</Text>
        </View>
    );
};

export default ListPlaces;
